(() => {
    "use strict";

    // The committed configuration is intentionally inert. The GitHub Pages
    // workflow replaces only the generated artifact with production IDs.
    window.__PORTFOLIO_ANALYTICS_CONFIG__ = Object.freeze({
        environment: "development",
        gaMeasurementId: "",
        clarityProjectId: ""
    });
})();
